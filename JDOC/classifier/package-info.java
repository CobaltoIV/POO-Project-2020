/**
 * The package conatins the framework necessary for building a classifier out of the training data.
 * The classifier also contains the method {@link classifier.Classifiers#predict(data.InputHandler)}
 * which allows the user to test the classifier. 
 */

package classifier;
